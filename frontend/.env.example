# Backend base URL used by the frontend proxy route.
# Set this to your deployed backend in staging/production.
HIVE_BACKEND_URL=http://localhost:8000

# Optional: frontend proxy prefix for backend calls.
# Leave as default unless you need a custom route namespace.
NEXT_PUBLIC_HIVE_PROXY_PREFIX=/api/hive
